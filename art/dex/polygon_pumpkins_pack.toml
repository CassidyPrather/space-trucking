# Written by `cargo xtask art describe`. One table per mesh in one pack.
#
# This is a CATALOGUE, not a manifest. `art/manifest.toml` is promises
# somebody typed and something checks; every `description` here is a
# sentence a vision model wrote about a picture of the mesh, and nothing
# in this repository can check it. Search it, read it, then look at the
# mesh — `described_by` says what saw it.
#
# It carries no art: names, digests, counts and English, which is the
# same kind of thing the manifest already holds. See docs/ART_PIPELINE.md.
#
#   name        the file's own stem, as the pack spells it
#   pack        the `[pack.*]` id the manifest declares, or the directory
#   source      where it is in that pack — paste this into a manifest
#   asset       the manifest id already naming this file, if one does
#   atlas       what the preview was painted with; a guess unless `asset`
#   textures    the images the preview scene actually bore
#   wanted      a texture this mesh named and the pack does not carry. Its
#               picture was painted with the nearest thing there was, so read
#               the description beside it with suspicion
#   differs     what sets it apart from the others of its name, written
#               from a second look at the whole family side by side
#   size        how big it is across each axis, in the file's own units

[mesh.sm_prop_pumpkin_lanturn_01]
name = "SM_Prop_Pumpkin_Lanturn_01"
pack = "polygon_pumpkins_pack"
source = "SourceFiles/FBX/SM_Prop_Pumpkin_Lanturn_01.fbx"
sha256 = "3039dc6270ff6c0e6cfdc44943e45e5d6e1ad775ce9aa11da6577e2c977f22df"
atlas = "POLYGON_Pumpkin_Tex.png"
textures = "POLYGON_Pumpkin_Tex.png"
description = "A squat, faceted orange gourd with a short green stem, shown in two states: a plain version and one with a carved, jagged jack-o'-lantern face featuring triangular eyes and a toothy grin. The low-poly style gives it a crisp, stylized look, and the dual variants make it versatile for both spooky and autumnal scenes."
differs = "Higher poly count with a shorter stem and non-glowing carved face, best for static autumnal decor."
described_by = "deepseek/deepseek-v4-flash-vision-exp"
triangles = 650
meshes = 1
materials = 1
size = [0.503887, 0.516315, 0.51628804]

[mesh.sm_prop_pumpkin_lanturn_02]
name = "SM_Prop_Pumpkin_Lanturn_02"
pack = "polygon_pumpkins_pack"
source = "SourceFiles/FBX/SM_Prop_Pumpkin_Lanturn_02.fbx"
sha256 = "2ab6b3a28850efb55457fa9230d55db13845bc2b767fa69c8100b7717050c9cc"
atlas = "POLYGON_Pumpkin_Tex.png"
textures = "POLYGON_Pumpkin_Tex.png"
description = "A squat, faceted orange gourd with a thick green stem, shown in two states: a plain uncarved version and one with a jagged, glowing jack-o'-lantern face cut into its front. The low-poly geometry gives it a crisp, stylized look, and the warm, emissive interior makes it ideal for spooky lighting in a Halloween scene."
differs = "Lower poly with a thicker stem and emissive glowing interior, ideal for dynamic spooky lighting."
described_by = "deepseek/deepseek-v4-flash-vision-exp"
triangles = 536
meshes = 1
materials = 1
size = [0.503887, 0.516315, 0.51628804]
